<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE logback>
<configuration>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<charset>UTF-8</charset>
			<pattern>%d{HH:mm:ss} %-5level [%thread] %logger{100} : %line %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="SYSTEM" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${catalina.home}/logs/travel5#ras.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${catalina.home}/logs/archive/domestic.%d{yyyy-MM-dd}.log</fileNamePattern>

			<maxHistory>30</maxHistory>
			<totalSizeCap>10MB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<charset>UTF-8</charset>
			<pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{100} : %line %msg%n</pattern>
		</encoder>
	</appender>

	<logger name="org.quartz" level="INFO">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="SYSTEM" />
	</logger>

	<logger name="common" level="DEBUG">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="SYSTEM" />
	</logger>

	<logger name="a00100" level="DEBUG">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="SYSTEM" />
	</logger>

</configuration>
